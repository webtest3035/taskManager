{
  "name": "taskManager",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}